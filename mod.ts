import paserServer from './config/server.ts';

// Re-export everything from phaser:
export * from 'https://cdnjs.cloudflare.com/ajax/libs/phaser/3.87.0/phaser.esm.min.js';

// Export `paserServer` properly:
export { paserServer };
